---
const navItems = [
	{ href: '/', label: 'Home' },
	{ href: '/about', label: 'About' },
	{ href: '/garments', label: 'Garments' },
	{ href: '/process', label: 'Process' },
	{ href: '/contact', label: 'Contact' },
];

const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';
---

<header class="sticky top-0 z-50 border-b border-[#2c271f]/10 bg-[#efe8db]/95 backdrop-blur">
	<div class="mx-auto flex max-w-7xl items-center justify-between gap-5 px-5 py-4 sm:px-6 lg:px-8">
		<a href="/" class="flex min-w-0 items-center gap-3" aria-label="Wild Balance home">
			<span class="flex h-10 w-10 shrink-0 items-center justify-center border border-[#3a382f]/25 bg-[#d8cdbb] text-[0.68rem] font-semibold tracking-[0.18em] text-[#2c271f]">
				WB
			</span>
			<span class="min-w-0">
				<span class="block text-sm font-semibold uppercase tracking-[0.2em] text-[#2c271f]">Wild Balance</span>
				<span class="mt-1 hidden text-xs text-[#6f6658] sm:block">Cut and sewn in Christchurch</span>
			</span>
		</a>

		<nav class="hidden items-center gap-1 md:flex" aria-label="Primary navigation">
			{
				navItems.map((item) => {
					const itemPath = item.href.replace(/\/$/, '') || '/';
					const isActive = pathname === itemPath;

					return (
						<a
							href={item.href}
							aria-current={isActive ? 'page' : undefined}
							class:list={[
								'px-3 py-2 text-sm transition-colors',
								isActive
									? 'text-[#2c271f]'
									: 'text-[#6f6658] hover:text-[#2c271f]',
							]}
						>
							{item.label}
						</a>
					);
				})
			}
		</nav>

		<a
			href="/contact"
			class="hidden border border-[#3a382f]/25 bg-[#f8f3e9] px-4 py-2 text-sm font-medium text-[#2c271f] transition-colors hover:border-[#3a382f]/45 hover:bg-[#e2d7c4] md:inline-flex"
		>
			Waitlist
		</a>
	</div>

	<nav class="flex gap-1 overflow-x-auto border-t border-[#2c271f]/10 px-4 py-2 md:hidden" aria-label="Mobile navigation">
		{
			navItems.map((item) => {
				const itemPath = item.href.replace(/\/$/, '') || '/';
				const isActive = pathname === itemPath;

				return (
					<a
						href={item.href}
						aria-current={isActive ? 'page' : undefined}
						class:list={[
							'whitespace-nowrap px-3 py-2 text-sm transition-colors',
							isActive ? 'text-[#2c271f]' : 'text-[#6f6658]',
						]}
					>
						{item.label}
					</a>
				);
			})
		}
	</nav>
</header>
